# Main config for handling the configs for general components/ and parts 
# to general, (tournament details etc) 
#
defaults:
  - links: general 

# HACK: - extend this 
scrape:
  max_workers: 3
#  match_active_components: ["base", "stats", "lineup", "incidents"] 
  match_active_components: ["base", "lineup", "incidents", "odds"] 

  # match_active_components: ["base", "stats", "lineup", "incidents", "odds"] 
  # match_active_components: ["base", "lineup", "incidents", "odds"]
  # match_active_components: ["base", "stats", "lineup", "odds"]
  # active_components: ["base", "stats", "lineup", "incidents", "graph"]
  wait_time_after_single_scrape: 3




# Details 
details:
  name: "general_main"
  links: "links/general"
  type: "default"

